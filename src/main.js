// import './assets/main.css'

import { createApp } from 'vue'
import App from './App.vue'
import router from './Router' // Importamos el archivo que creamos arriba

createApp(App).use(router).mount('#app')
